export enum enumGameState {
    Init,
    ChooseCharacter,
    Rest,
    BattleStart,
    Battle,
    BattleEnd,
    GameEnd
}

export enum enumBattleResult {
    Init,
    Draw,
    PlayerWin,
    PlayerLose
}

export enum enumRoundState {
    Start,
    Counting,
    Duel,
    Settle,
}
